<h3><%= @category_id.name %>カテゴリ</h3>
<%= @category_id.name %>
<% @category.each do |category| %>
  <br><iframe width="560" height="315" src="<%= 'https://www.youtube.com/embed/' + category.video_id %>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
<% end %>